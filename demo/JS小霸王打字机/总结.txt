keyCode
	function（e）{
		console.log（e.keyCode）
	}
打印的是ASCII码（也就是对应的数字）

*********************************************************************
String.fromCharCode(ASCII码)           把ASCII码转化为对应的字符
***********************************************************************
dom.className=' 类名 类名 类名';
可写多个类名，要用逗号隔开